{
  "name": "safety-platform-prototype",
  "version": "5.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "serve": "python -m http.server 8080"
  },
  "dependencies": {
    "@playwright/test": "^1.60.0",
    "playwright": "^1.60.0"
  }
}
