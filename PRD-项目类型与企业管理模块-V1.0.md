# 项目类型与企业管理模块需求文档

## 版本信息
- 版本号：V1.0
- 创建日期：2026-04-28
- 最后更新：2026-04-29

---

## 1. 页面布局

### 1.1 整体布局
- **三栏式布局**
  - 左侧：树形筛选区（固定宽度 280px）
  - 中间：主内容区（自适应宽度）
  - 右侧：预留扩展区（默认隐藏）

### 1.2 左侧树形筛选区
- **Tab 切换**：街道树 / 领域树
- **搜索框**：支持按名称关键字搜索树节点
- **树形结构**：
  - 街道树：区 → 街道 → 社区 → 网格（四级）
  - 领域树：7 大类 → 小类
- **节点显示**：节点名称 + 项目数量角标
- **交互**：点击节点筛选右侧列表，支持展开/折叠

### 1.3 主内容区
从上到下依次为：

#### 1.3.1 统计卡片区（4 卡片横向排列）
| 卡片名称 | 显示内容 |
|---------|---------|
| 全部项目 | 总数量 |
| 在建项目 | 项目类型为"在建项目"的数量 |
| 运营项目 | 项目类型为"运营项目"的数量 |
| 已归档 | 项目状态为"已归档"的数量 |

#### 1.3.2 面包屑导航
- 显示当前选中的树节点路径
- 格式：当前筛选：{节点路径}
- 支持点击清除返回全部

#### 1.3.3 查询区
| 查询条件 | 组件类型 | 说明 |
|---------|---------|------|
| 项目名称 | 文本输入框 | 支持模糊查询 |
| 项目编号 | 文本输入框 | 支持模糊查询 |
| 项目状态 | 下拉选择框 | 全部/筹备/在建/完工/运营/归档 |
| 所属督导组 | 下拉选择框 | 从督导组字典选择 |

- **查询按钮**：主按钮样式，点击执行查询
- **重置按钮**：次按钮样式，点击清空查询条件

#### 1.3.4 操作按钮区
| 按钮 | 功能 |
|-----|------|
| 新增项目 | 打开新增弹窗 |
| 批量导入 | 打开批量导入弹窗（预留） |
| 导出数据 | 导出当前查询结果到 Excel |

#### 1.3.5 列表区（表格）
**表格列定义：**

| 列名 | 字段 | 宽度 | 说明 |
|-----|------|-----|------|
| 项目编号 | projectNo | 120px | - |
| 项目名称 | projectName | 200px | 超出显示省略号，悬浮提示完整内容 |
| 项目类型 | projectCategoryName | 100px | 在建项目/运营项目 |
| 领域小类 | domainName | 120px | - |
| 项目状态 | projectStatusName | 100px | 带状态标签样式 |
| 所属区划 | districtFullName | 180px | 区/街道/社区/网格 |
| 创建时间 | createTime | 160px | 格式：YYYY-MM-DD HH:mm |
| 操作 | - | 240px | 固定列，右侧悬浮 |

**操作列按钮（4 个蓝色按钮，从左到右）：**
| 按钮 | 功能 | 图标/文字 |
|-----|------|---------|
| 编辑 | 打开编辑弹窗 | 编辑 |
| 删除 | 打开删除确认弹窗 | 删除 |
| 详情 | 打开详情弹窗 | 详情 |
| 重置密码 | 打开重置密码确认弹窗 | 重置密码 |

**表格样式规范：**
- 表头：浅灰色背景，黑色文字，加粗
- 数据行：白色背景，hover 浅灰色
- 操作列：白色实色背景（非透明），sticky 固定右侧
- 文本溢出：单行显示，超出部分省略号，悬浮显示完整内容（title 属性）
- 斑马纹：偶数行浅灰色背景

#### 1.3.6 分页区
- 左侧：共 X 条，第 Y/Z 页
- 中间：分页按钮（上一页、页码、下一页）
- 右侧：跳至 [输入框] 页 [确定按钮]

---

## 2. 弹窗设计

### 2.1 新增/编辑项目弹窗

#### 2.1.1 弹窗规格
- 宽度：900px
- 高度：自适应，最大高度 90vh，超出滚动
- 标题：新增项目 / 编辑项目

#### 2.1.2 布局结构
**单栏布局**，从上到下依次为：

**模块一：基础信息**
| 字段 | 组件 | 必填 | 说明 |
|-----|------|-----|------|
| 项目类型 | 单选按钮组 | 是 | 在建项目 / 运营项目，切换影响后续字段显示 |
| 项目编号 | 文本输入框 | 是 | 系统自动生成，支持手动修改，格式：PRJ-年月日-序号 |
| 项目名称 | 文本输入框 | 是 | 最长 50 个字符 |
| 领域小类 | 级联选择器 | 是 | 先选大类，再选小类 |
| 项目小类 | 下拉选择框 | 否 | 仅在建项目显示，根据领域小类联动 |
| 运营状态 | 下拉选择框 | 否 | 仅运营项目显示 |
| 项目状态 | 下拉选择框 | 是 | 筹备/在建/完工/运营/归档 |
| 所属区划 | 级联选择器 | 是 | 四级：区 → 街道 → 社区 → 网格 |
| 详细地址 | 文本输入框 | 是 | 最长 100 个字符 |
| 所属督导组 | 下拉选择框 | 是 | 从督导组字典选择 |
| 项目描述 | 文本域 | 否 | 最长 500 个字符 |

**模块二：四方责任体**（在建项目）/ **运营单位**（运营项目）

*建设单位、施工单位、监理单位（每行一个单位，共三行）：*
| 字段 | 组件 | 必填 | 说明 |
|-----|------|-----|------|
| 单位名称 | 文本输入框 + 选择按钮 | 是 | 可手动输入或点击"选择"按钮从企业库选择 |
| 统一社会信用代码 | 文本输入框 | 否 | 选择企业后自动回填 |
| 联系人 | 文本输入框 | 是 | 手动输入 |
| 联系电话 | 文本输入框 | 是 | 手动输入 |

*分包单位（动态多行）：*
- 可点击"新增分包"按钮添加多行
- 每行字段同建设单位：单位名称（带选择按钮）、统一社会信用代码、联系人、联系电话
- 每行右侧有"删除"按钮
- **分包单位可以全部删除**（与建设单位逻辑一致）

**模块三：账号信息**（仅新增时显示）
| 字段 | 组件 | 必填 | 说明 |
|-----|------|-----|------|
| 登录账号 | 文本输入框 | 是 | 4-32位字母或数字，编辑时禁用 |
| 初始密码 | 文本输入框 | 是 | 默认123456，编辑时隐藏该字段 |

#### 2.1.3 底部按钮
- 取消：关闭弹窗，不保存
- 确认保存：验证表单，保存数据，刷新列表

### 2.2 详情弹窗

#### 2.2.1 弹窗规格
- 宽度：900px
- 标题：项目详情

#### 2.2.2 布局结构
**单栏布局**（与编辑页面保持一致），只读展示：

**模块一：基础信息**
- 项目编号、项目名称、项目类型、领域小类
- 项目小类/运营状态（根据项目类型显示）
- 项目状态（带标签样式）
- 所属区划（四级完整路径）
- 详细地址、所属督导组
- 项目描述

**模块二：四方责任体/运营单位**
- 建设单位、施工单位、监理单位：单位名称 / 联系人 / 联系电话
- 分包单位：多行展示，每行同建设单位格式

**模块三：监管单位**
- 行业监管：{领域小类}监管局
- 专业监管：{督导组名称}
- 属地监管：{区名}应急管理局

#### 2.2.3 底部按钮
- 关闭：关闭弹窗

### 2.3 删除确认弹窗
- 宽度：400px
- 图标：红色警告图标
- 标题：确认删除
- 内容：确定要删除项目「{项目名称}」吗？
- 提示：删除后数据无法恢复，请谨慎操作
- 按钮：取消、确认删除（红色）

### 2.4 重置密码确认弹窗
- 宽度：400px
- 图标：黄色警告图标
- 标题：重置密码
- 内容：确定要重置项目「{项目名称}」的密码吗？
- 提示：密码将重置为默认密码：123456
- 按钮：取消、确认重置

### 2.5 企业选择弹窗

#### 2.5.1 弹窗规格
- 宽度：700px
- 标题：选择企业

#### 2.5.2 布局结构
- 搜索区：企业名称/统一社会信用代码关键字搜索
- 列表区：
  - 表头：序号、企业名称、统一社会信用代码、操作
  - 数据行：展示企业信息
- 分页区：分页信息 + 分页按钮 + 跳转

#### 2.5.3 选择逻辑
- 点击操作列"选择"按钮
- 回填企业名称和统一社会信用代码到对应字段（建设单位/施工单位/监理单位/分包单位）
- 联系人、联系电话需手动输入（不从企业库带回）
- 关闭企业选择弹窗

---

## 3. 交互逻辑

### 3.1 项目类型切换
- 选择"在建项目"：显示项目小类、四方责任体模块
- 选择"运营项目"：显示运营状态、运营单位模块
- 切换时清空已填写的关联字段

### 3.2 四级区划联动
- 选择区 → 加载街道选项 → 清空社区、网格
- 选择街道 → 加载社区选项 → 清空网格
- 选择社区 → 加载网格选项

### 3.3 领域小类联动
- 选择领域小类 → 加载对应的项目小类选项（仅在建项目）

### 3.4 树形筛选
- 点击树节点 → 高亮选中节点 → 面包屑显示路径 → 列表筛选数据
- 点击父节点 → 展示所有子节点对应的数据
- 切换 Tab → 清空选中状态 → 展示全部数据

### 3.5 表格操作
- 悬浮行 → 行背景色变化
- 点击操作按钮 → 执行对应操作
- 操作列固定 → 横向滚动时始终可见

---

## 4. 样式规范

### 4.1 颜色规范
| 用途 | 颜色值 |
|-----|-------|
| 主色（蓝色） | #1677ff |
| 主色悬浮 | #4096ff |
| 成功/通过 | #52c41a |
| 警告/待处理 | #faad14 |
| 错误/不通过 | #ff4d4f |
| 边框色 | #d9d9d9 |
| 分隔线 | #f0f0f0 |
| 背景灰 | #f5f5f5 |
| 标题文字 | #1f1f1f |
| 正文文字 | #333333 |
| 次要文字 | #666666 |
| 禁用文字 | #999999 |

### 4.2 字体规范
- 主字体：系统默认无衬线字体（-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial）
- 标题字号：16px，font-weight: 600
- 正文字号：14px，font-weight: 400
- 辅助文字：13px 或 12px

### 4.3 间距规范
- 页面内边距：24px
- 模块间距：20px
- 表单行间距：16px
- 表单标签与控件间距：6px
- 按钮间距：12px
- 表格行高：48px
- 表格单元格内边距：12px 16px

### 4.4 圆角规范
- 卡片/模块：8px
- 按钮：4px
- 输入框：4px
- 标签：4px
- 弹窗：8px

### 4.5 阴影规范
- 卡片默认：无阴影，边框分隔
- 卡片悬浮：box-shadow: 0 2px 8px rgba(0,0,0,0.08)
- 弹窗：box-shadow: 0 4px 16px rgba(0,0,0,0.12)
- 下拉菜单：box-shadow: 0 2px 8px rgba(0,0,0,0.08)

---

## 5. 数据模型

### 5.1 项目对象（Project）
```javascript
{
  id: String,                    // 项目ID
  projectNo: String,             // 项目编号
  projectName: String,           // 项目名称
  projectCategory: String,       // 项目类型：1在建 2运营
  domainId: String,                // 领域小类ID
  domainName: String,              // 领域小类名称
  projectType: String,             // 项目小类（在建项目）
  operationStatus: String,         // 运营状态（运营项目）
  projectStatus: String,           // 项目状态
  districtCode: String,          // 区代码
  districtName: String,          // 区名称
  streetCode: String,            // 街道代码
  streetName: String,            // 街道名称
  communityCode: String,         // 社区代码
  communityName: String,         // 社区名称
  gridCode: String,              // 网格代码
  gridName: String,              // 网格名称
  address: String,               // 详细地址
  supervisionGroupId: String,    // 督导组ID
  supervisionGroupName: String,  // 督导组名称
  description: String,           // 项目描述
  parties: Array,                // 四方责任体
  loginAccount: String,          // 登录账号
  createTime: Date               // 创建时间
}
```

### 5.2 责任体对象（Party）
```javascript
{
  partyType: Number,      // 1建设单位 2施工单位 3监理单位 4分包单位
