# 变更日志 (CHANGELOG)

所有版本的变更记录。

## modules/government/gov-org.html

### V7.0.11-review-fix2 - 2026-06-18
- [评审修复] 第二轮评审 2 项 LOW 代码质量优化
  - **L2** `saveForm()` 三元运算两边相同的冗余简化：`const targetId = isEditing ? selectedNode.id : selectedNode.id` → `const targetId = selectedNode.id`
  - **L3** `showAddChildModal()` 删除冗余的 `buildParentOptions(formType.value, selectedNode.id, null)` 调用——紧接其后 `onTypeChange()` 内部会再次调用 `buildParentOptions(type, currentParent, ...)`，效果相同
- [影响] 文件 ~935 行；HTTP 200，46,912 字节；无行为变化（仅清理）

### V7.0.11-review-fix - 2026-06-18
- [评审修复] PRD↔原型对齐评审后落地 3 项原型修改（H4/M1/M3）
  - **H4** 删除确认改为自定义弹窗：原 `confirm(...)` 替换为 `showConfirm(message, onConfirm, title)` + `closeConfirmModal()` 通用函数；新增 `#confirmModal` 弹窗（420px 宽，复用 `.modal-overlay` 框架）；统一与超管端弹窗风格
  - **M1** 本单位详情隐藏「上级组织」行：`renderDetail()` html 模板中`上级组织` 行加 `${selectedNode.isRoot ? '' : ...}` 条件渲染（PRD §6.2.8.3.5 本单位行未列此字段，根节点逻辑上无父级）
  - **M3** `closeModal()` 补 `isEditing = false`：防止下次新增时 isEditing 残留导致状态泄漏
- [影响] 文件 ~937 行；HTTP 200，47,007 字节
- [关联] 同步修改 PRD V7.0.10：§6.2.8.3 详情字段精简、本单位按钮改为不渲染、统计口径调整（详见 PRD CHANGELOG）

### V7.0.10-spacing2 - 2026-06-18
- [样式] 收紧 split-view 外边距，让左右两侧贴边、底部更紧凑
  - `.split-view` padding 由 `var(--spacing-md)` (16px 四周) → `var(--spacing-md) 8px 8px`（顶 16px / 左右 8px / 底 8px）
  - `.split-view` gap 保持 `20px`（左右两个面板之间的空隙不变）
- [影响] 文件 883 行；HTTP 200，45,540 字节

### V7.0.10-spacing - 2026-06-18
- [样式] 修正详情区头部「标题 ↔ 操作区」之间过大的水平空白
  - `.detail-header` 由 `justify-content: space-between` → `gap: 24px`
  - `.detail-heading` 增加 `flex: 0 1 auto`，让标题与操作按钮紧贴而非分占两端
  - 解决本单位场景下（操作区仅剩 [新增] 单按钮时）按钮被推到最右侧、与标题之间出现大段空白的视觉问题
- [影响] 文件 883 行；HTTP 200，45,532 字节

### V7.0.10-trim2 - 2026-06-18
- [精简] 去除详情区右上 action-tip 与详情字段冗余
  - 移除：操作栏 `.action-tip` 文案区（「🔒 本单位由超管端维护，仅支持在本单位下新增下级」/「⚠️ 已达 3 级科室...」/「✓ N 级科室：可新增...」全部移除）
  - 操作按钮：从 `disabled` 灰显改为 `display:none` **直接隐藏**——本单位仅显示 [新增]，3 级科室仅显示 [编辑][删除]，其他节点显示 [新增][编辑][删除]
  - 基本信息区移除：**所属区划** 行（含 🔒 锁定标识）
  - 基本信息区移除：**来源** 行（含 🔒 锁定标识）
- [影响] 文件 902 → 883 行；HTTP 200，45,561 字节；保留的「本单位由超管端维护」仅作为 JS edge-case toast 兜底（按钮已隐藏，正常路径不触发）

### V7.0.10-trim - 2026-06-18
- [精简] 去除页面冗余字段
  - 详情区移除：**扩展信息** 行（PRD §6.2.8.3.5 不在字段清单内）
  - 详情区移除：**最近同步时间** 行（与「来源」字段重叠，已锁定为同步源即可）
  - 弹窗移除：**所属区划** 只读输入框 + 提示文案（PRD §6.2.8.3.4.1 9 字段不含此项）
  - 弹窗移除：所有 `.form-hint` 提示文案
    - 「编辑时上级组织置灰不可改（PRD §6.2.8.3.4.1）」
    - 「3 级科室不可再增子科室」
    - 「字典与超管端 §6.1.1.3.2.1 一致」
    - 动态提示「下级单位可挂在本单位或上级下级单位下 / 科室可挂在单位或 1-2 级科室下，最多 3 级」
  - 详情区移除：本单位关联管理员账号卡片底部的 `.form-hint` 跳转说明
  - `onTypeChange()` 移除 `parentHint` 文案更新逻辑
- [影响] 文件 925 → 902 行；HTTP 200，46,905 字节；保留 PRD §6.2.8.3 业务规则不变（3 级深度校验、本单位锁定、同级名称校验仍由 JS 逻辑兜底，不再用文案兜底）

### V7.0.10-style-align - 2026-06-18
- [样式] 与超管端 `super-admin/gov-org.html` 完全一致的页面框架与弹窗样式
  - 接入：`css/variables.css` + `css/framework.css`（替代原内联完整样式）
  - 容器：`<div class="page-container" data-module="gov-org">`（与超管端 `data-module="sa-gov-org"` 同模式）
  - 顶部：`.breadcrumb`「当前位置：政府端 › 组织架构管理」（替代原 `.page-header`）
  - 布局：左 `.tree-panel`（320px 固定）+ 右 `.detail-panel`（统计卡 inside top → detail-header → detail-body）
  - 统计卡：3 张移入右面板顶部（与超管端布局一致），图标渐变背景 + hover 抬升动效
  - 树节点：JS 动态渲染（`renderTree`/`renderNodes`），`--level` CSS 变量驱动缩进；保留「本单位 / 下级单位 / N 级科室」badge
  - 详情区：`.info-section` + `.info-grid`（2 列）+ `.account-card`（本单位独有：关联管理员账号）
  - 操作按钮：`.btn-link` 风格（新增/编辑/删除），按节点类型动态启用，`.action-tip` 文案带 `warn` 状态
- [弹窗] 与超管端 modal 同框架，宽度 560px
  - 结构：`.modal-overlay` > `.modal-dialog` > header/body/footer
  - 表单：`.form-row` 双列布局 + `.form-group` + `.form-input`/`.form-select`
  - 状态：`.form-switch` + `.switch-track`/`.switch-text`（与超管端开关一致）
  - 政府端字段裁剪：**保留** 上级组织 / 组织类型 / 名称 / 单位类型 / 负责人 / 联系电话 / 邮箱 / 排序 / 状态 + 只读「所属区划」（深圳市/南山区）；**移除** 超管端「生成单位超管账号」选项（PRD §6.2.8 政府端不在此页创建管理员账号）
  - 业务规则：编辑时「上级组织 + 组织类型」置灰；3 级科室禁止新增子科室；同级名称重复校验；本单位（root，sourceType=sync）不可编辑/删除
- [反馈] 提示从原生 `alert` 切换至 `showToast()`（与超管端一致）
- [影响] 文件 980 → 925 行；HTTP 200，48,588 字节；模块 ID `gov-org`，资产路径 `../../css/*.css` + `../../js/annotations.js`

### V7.0.9-data-align - 2026-06-18
- [对齐] 政府端组织架构数据与超管端 §6.1.1 NS-AMB 节点完全一致
  - 本单位：「XX 区应急管理局」（虚构）→ **「区应急管理局」**（与超管端 NS-AMB 同步）
  - 直属下级：3 个科室 + 3 个下级单位（虚构）→ **6 个 1 级科室**（NS-AMB 真实子节点：督导检查科 / 综合协调科 / 区三防办 / 安全监管和执法科 / 防灾减灾科 / 危化品监管和执法科）
  - 移除：所有「下级单位」节点（NS-AMB 在超管端 sourceType=sync，下属仅 dept 类型）、所有 2/3 级科室节点（NS-AMB 真实数据中科室无嵌套）
  - 关联管理员账号：王建国（虚构）→ **nsamb-admin / 张主任 / 138****1002 / 最近登录 2026-04-02 09:30**
  - 所属区划：深圳市/XX 区 → **深圳市 / 南山区**
  - 排序号：1 → **2**（与超管端 NS-AMB sort=2 一致）
  - 扩展信息：补「重点监管单位；平台侧维护联系人与账号策略」
  - 统计卡默认值：5/14/6 → **1/6/1**（单位/科室/政府管理员）
  - 弹窗「上级组织」下拉：6 项（虚构层级）→ **7 项**（本单位 + 6 个 1 级科室）
- [影响] 政府端 ADMIN 视角下展示的本单位与超管端「区应急管理局」节点数据保持单一事实源；删除/编辑入口仍受 PRD §6.2.8.3.4 业务规则约束（外部同步节点，本单位仅可在其下新增）

### V7.0.9-rewrite - 2026-06-18
- [重写] 按 PRD §6.2.8.3 全量对齐
  - 统计卡：4 张（本单位/科室数量/在岗人员/岗位定义） → **3 张**（单位总数/科室总数/政府管理员数）
  - 组织树：「本单位→科室→岗位」3 级 → **「本单位（根，只读）→ 下级单位（递归嵌套）→ 1-3 级科室」**，引入「下级单位」概念，节点带类型 badge（本单位/下级单位/N 级科室）
  - 操作按钮区：新增 [新增][编辑][删除]，按选中节点动态启用——本单位仅 [新增]、3 级科室禁用 [新增]、其他节点全启用；带提示文案（§6.2.8.1.3 + §6.2.8.3.4.2）
  - 当前路径区：面包屑展示选中节点完整路径
  - 基本信息区：补单位类型字段（委办局/街道办/督导组/其他）、来源字段（外部同步/手工新增，灰显只读）、所属区划（灰显只读）、最近同步时间
  - 新增/编辑组织弹窗：完整 9 字段（§6.2.8.3.4.1），含上级组织级联、组织类型动态切换（单位/科室）、单位类型下拉、电话/邮箱格式校验、3 级科室约束、编辑时上级组织/组织类型置灰
  - 删除校验：拒绝删除本单位、拒绝删除有子组织节点（BR-GOV-ORG-05）
  - 统计卡口径：随选中节点变化（§6.2.8.3.3.1 选中节点对统计口径的影响表）
  - 移除：岗位列表、人员列表（不属本模块——岗位属「职务管理」、人员属 §6.2.8.4 人员账号管理）
  - 移除：审批列表 badge（PRD §6.2.8 无审批列表概念）
- [影响] module-registry.json gov-org 条目：prd → `§6.2.8.3 组织架构管理（政府端）`、status → `in-review`

## index.html

### V2.1.1 - 2026-04-25
- [修复] 导航栏样式微调

### V2.1.0 - 2026-04-25
- [新增] 版本管理功能测试
- [优化] 页面加载性能

## ent-info.html

### V1.2.0 - 2026-04-25
- [新增] 企业信息表单
- [修复] 字段验证问题

## ent-org.html

### V1.2.0 - 2026-04-25
- [新增] 组织架构树
- [优化] 拖拽排序

